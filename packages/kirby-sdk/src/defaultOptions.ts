import { KirbyNamespace } from './kirby';

const defaultOptions: KirbyNamespace.Options = {
  appKey: null,
  url: null,
  uuid: null,
  jsErr: true,
  resErr: true,
  ajaxErr: true,
  consoleErr: true,
  scriptErr: true,
};

export default defaultOptions;
